language: python
python:
  - '3.8'

before_install:
  - python -m pip install poetry
install:
  - poetry install
env: DJANGO_SETTINGS_MODULE="config.settings.local"
services:
  - postgresql
script:
  - ./manage.py test -v 2
  - black . --check
  - isort . --check
